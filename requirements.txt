fastapi==0.111.0
uvicorn[standard]==0.29.0
httpx==0.27.0
python-dotenv==1.0.1
pydantic==2.7.0

# NOTE: The memory system (memory.py) is not yet wired into main.py.
# Its heavy dependencies are deferred to keep Railway builds fast (~2.5GB otherwise).
# When memory.py is integrated, add:
#   chromadb==0.5.0
#   sentence-transformers==3.0.0
#   torch (CPU-only build recommended for Railway)
